spring.application.name=travellplaner
spring.application.name=order.service
spring.profiles.active=stage

server.port=8082

spring.datasource.url=jdbc:mysql://localhost:3306/travel_planner_db
spring.datasource.username=root
spring.datasource.password=Root@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
<<<<<<< HEAD
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Application Configuration
spring.application.name=travel-planner-backend

# Spring MVC Configuration
spring.mvc.pathmatch.matching-strategy=ant_path_matcher

# JWT Configuration
jwt.secret-key=your-secret-key-for-travel-planner-application-2026-encryption
jwt.expiration-ms=86400000
jwt.issuer=travel-planner-app

# Logging
logging.level.org.springframework=INFO
logging.level.com.travelplanner=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Scheduler Configuration
spring.task.scheduling.pool.size=10

# Notification Settings
notification.default.quiet-hours-start=22
notification.default.quiet-hours-end=7
notification.flight-check-interval=60 # minutes
notification.weather-check-interval=360 # minutes

# Offline/Cache Configuration
offline.cache.expiration-days=7
offline.cache.cleanup-enabled=true
offline.export.expiration-hours=24
offline.sync.conflict-resolution=SERVER_WINS # SERVER_WINS, CLIENT_WINS, MANUAL

# File Export Configuration (for future implementation)
#offline.export.storage-path=./exports
#offline.export.max-file-size=10485760 # 10MB
=======
spring.jpa.show-sql=true
>>>>>>> 6724e6d (Updated home page UI and assets)
